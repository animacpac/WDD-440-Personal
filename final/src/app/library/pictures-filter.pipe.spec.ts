import { PicturesFilterPipe } from './pictures-filter.pipe';

describe('PicturesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PicturesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
